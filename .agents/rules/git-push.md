# Git Push Policy

- **CRITICAL**: Never run `git push` (or push code to any remote repository) without explicit approval or instruction from the user.
- Local commits, builds, type-checks, and code modifications are permitted, but code must stay local until the user explicitly requests or authorizes a push.
